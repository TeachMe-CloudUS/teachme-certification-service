---
parameters:
  - name: data
    in: body
    required: true
    schema:
      type: object
      properties:
        student_id:
          type: string
        student_userId:
          type: string
        student_name:
          type: string
        student_surname:
          type: string
        student_email:
          type: string
        course_id:
          type: string
        course_name:
          type: string
        course_description:
          type: string
        course_duration:
          type: string
        course_level:
          type: string
        completionDate:
          type: string
          format: date
definitions:
  Student_Course_Data:
    type: object
    properties:
      student_id:
        type: string
      student_userId:
        type: string
      student_name:
        type: string
      student_surname:
        type: string
      student_email:
        type: string
      course_id:
        type: string
      course_name:
        type: string
      course_description:
        type: string
      course_duration:
        type: string
      course_level:
        type: string
      completionDate:
        type: string
  Response:
    type: object
    properties:
      success:
        type: boolean
        description: Indicates if the certification process was successful.
        example: true
      Error:
        type: string
        description: Error message in case of failure.
        example: "Certification failed: Certificate already exists"
      Reason:
        type: string
        description: The reason for failure.
        example: "Certificate already exists"
responses:
  201:
    description: Certification was successful and the certificate was generated.
    schema:
      $ref: '#/definitions/Response'
    examples:
      success: true
  400:
    description: Bad request - invalid data provided.
    schema:
      $ref: '#/definitions/Response'
    examples:
      Error: "Certification failed: Invalid data"
  409:
    description: Conflict - The certificate already exists.
    schema:
      $ref: '#/definitions/Response'
    examples:
      Error: "Certification failed"
      Reason: "Certificate already exists"
